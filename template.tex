\documentclass[hidelinks]{article}

\usepackage{hyperref}
\usepackage{fontspec}
\usepackage[english]{babel}
\usepackage{microtype}
\usepackage[english]{selnolig}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{titlesec}

\hypersetup{
  pdftitle={Angel Ouellet Resume},
  pdfauthor={Angel Ouellet}
}

\geometry{letterpaper, margin=1in}

\definecolor{angelpurp}{HTML}{7F12A3}

\newcommand{\leftymargin}{0.75in}

% Use specific font
\newfontfamily{\secfamily}{Work Sans}[Ligatures=TeX]
\newfontfamily{\titlefamily}{Gentium Plus}
\setmainfont[Numbers=Lining]{Gentium Book Plus}


%% Make headings purplz
\titleformat{\section}{\normalfont\Large\bfseries\color{angelpurp}\secfamily}{\thesection}{1em}{}

% Pandoc uses a \tightlist command to make the lists more compact. Since we're
% not using the default template, we need to add support for it ourselves.
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Disable numbering of sections
\setcounter{secnumdepth}{0}

% Disable indent
\setlength{\parindent}{0pt}

\begin{document}

%% Purple header with Angel's Name
\begin{tikzpicture}[remember picture, overlay]
  \node[yshift=-1.5in] at (current page.north west){
    \begin{tikzpicture}[remember picture, overlay]
      \fill [angelpurp] (0,0) rectangle (\paperwidth,1.5in);
      \node[anchor=west, xshift=\leftymargin, yshift=0.75in] {{\Huge\color{white}\textbf{\secfamily Angel Ouellet}}};
      \node[anchor=west, xshift=\leftymargin, yshift=0.75in-20pt] {\large\color{white}\secfamily\href{mailto:resume@jouellet.net}{resume@jouellet.net}};
    \end{tikzpicture}
  };
\end{tikzpicture}

\vspace{0.75in}

$abstract$

\section{Work History}
$for(jobs)$
\textbf{\titlefamily $it.jobtitle$}\\
\textit{\titlefamily $it.name$}\hfill $it.dates$
$it.bullets$
$endfor$

\newpage

\section{Skills}
\begin{itemize}
\tightlist$for(skills)$
        \item \textbf{\titlefamily $it.name$:} $it.list$
$endfor$
\end{itemize}

\section{Competitions}
$for(competitions)$
\textbf{\titlefamily $it.name$}
$it.bullets$
$endfor$

\section{Volunteering}
$for(volunteering)$
\textbf{\titlefamily $it.name$}
$it.bullets$
$endfor$


\end{document}
